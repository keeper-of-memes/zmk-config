From 6812a4f27e27e5b45cac3be5a341ab400af3e6df Mon Sep 17 00:00:00 2001
From: Ross <stag_chows_4i@icloud.com>
Date: Tue, 19 May 2026 14:02:53 +0100
Subject: [PATCH] Port nice_view_gem to LVGL 9 / ZMK Zephyr 4.1

---
 boards/shields/nice_view_gem/assets/crystal.c | 80 ++++++++----------
 boards/shields/nice_view_gem/assets/images.c  | 40 ++++-----
 .../shields/nice_view_gem/widgets/battery.c   | 10 +--
 boards/shields/nice_view_gem/widgets/layer.c  |  2 +-
 boards/shields/nice_view_gem/widgets/output.c | 20 ++---
 .../shields/nice_view_gem/widgets/profile.c   |  6 +-
 boards/shields/nice_view_gem/widgets/screen.c |  6 +-
 .../nice_view_gem/widgets/screen_peripheral.c |  2 +-
 boards/shields/nice_view_gem/widgets/util.c   | 82 ++++++++++++++++---
 boards/shields/nice_view_gem/widgets/util.h   | 14 +++-
 boards/shields/nice_view_gem/widgets/wpm.c    | 16 ++--
 11 files changed, 161 insertions(+), 117 deletions(-)

diff --git a/boards/shields/nice_view_gem/assets/crystal.c b/boards/shields/nice_view_gem/assets/crystal.c
index 9da1b48..e0736af 100644
--- a/boards/shields/nice_view_gem/assets/crystal.c
+++ b/boards/shields/nice_view_gem/assets/crystal.c
@@ -62,9 +62,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_01 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -129,9 +128,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_02 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -196,9 +194,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_03 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -263,9 +260,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_04 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -330,9 +326,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_05 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -397,9 +392,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_06 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -464,9 +458,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_07 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -531,9 +524,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_08 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -598,9 +590,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_0
 };
 
 const lv_img_dsc_t crystal_09 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -665,9 +656,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_10 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -732,9 +722,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_11 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -799,9 +788,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_12 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -866,9 +854,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_13 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -933,9 +920,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_14 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -1000,9 +986,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_15 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
@@ -1067,9 +1052,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_CRYSTAL_1
 };
 
 const lv_img_dsc_t crystal_16 = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 69,
     .header.h = 68,
     .data_size = 620,
diff --git a/boards/shields/nice_view_gem/assets/images.c b/boards/shields/nice_view_gem/assets/images.c
index 1ec20bf..aa5fa21 100644
--- a/boards/shields/nice_view_gem/assets/images.c
+++ b/boards/shields/nice_view_gem/assets/images.c
@@ -21,9 +21,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_BOLT uint
 };
 
 const lv_img_dsc_t bolt = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 5,
     .header.h = 9,
     .data_size = 17,
@@ -48,9 +47,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_BT uint8_
 };
 
 const lv_img_dsc_t bt = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 12,
     .header.h = 15,
     .data_size = 38,
@@ -76,9 +74,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_BT_NO_SIG
 };
 
 const lv_img_dsc_t bt_no_signal = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 12,
     .header.h = 15,
     .data_size = 38,
@@ -105,9 +102,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_BT_UNBOND
 };
 
 const lv_img_dsc_t bt_unbonded = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 22,
     .header.h = 15,
     .data_size = 53,
@@ -133,9 +129,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_USB uint8
 };
 
 const lv_img_dsc_t usb = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 20,
     .header.h = 11,
     .data_size = 41,
@@ -162,9 +157,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_GAUGE uin
 };
 
 const lv_img_dsc_t gauge = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 33,
     .header.h = 10,
     .data_size = 58,
@@ -206,9 +200,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_GRID uint
 };
 
 const lv_img_dsc_t grid = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 67,
     .header.h = 33,
     .data_size = 305,
@@ -233,9 +226,8 @@ const LV_ATTRIBUTE_MEM_ALIGN LV_ATTRIBUTE_LARGE_CONST LV_ATTRIBUTE_IMG_PROFILES
 };
 
 const lv_img_dsc_t profiles = {
-    .header.cf = LV_IMG_CF_INDEXED_1BIT,
-    .header.always_zero = 0,
-    .header.reserved = 0,
+    .header.magic = LV_IMAGE_HEADER_MAGIC,
+    .header.cf = LV_COLOR_FORMAT_I1,
     .header.w = 31,
     .header.h = 3,
     .data_size = 20,
diff --git a/boards/shields/nice_view_gem/widgets/battery.c b/boards/shields/nice_view_gem/widgets/battery.c
index 62aea5b..e64cc5d 100644
--- a/boards/shields/nice_view_gem/widgets/battery.c
+++ b/boards/shields/nice_view_gem/widgets/battery.c
@@ -11,11 +11,11 @@ static void draw_level(lv_obj_t *canvas, const struct status_state *state) {
     char text[10] = {};
 
     sprintf(text, "%i%%", state->battery);
-    lv_canvas_draw_text(canvas, 26, 19, 42, &label_right_dsc, text);
+    canvas_draw_text(canvas, 26, 19, 42, &label_right_dsc, text);
 }
 
 static void draw_charging_level(lv_obj_t *canvas, const struct status_state *state) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
     lv_draw_label_dsc_t label_right_dsc;
     init_label_dsc(&label_right_dsc, LVGL_FOREGROUND, &pixel_operator_mono, LV_TEXT_ALIGN_RIGHT);
@@ -23,14 +23,14 @@ static void draw_charging_level(lv_obj_t *canvas, const struct status_state *sta
     char text[10] = {};
 
     sprintf(text, "%i%%", state->battery);
-    lv_canvas_draw_text(canvas, 26, 19, 35, &label_right_dsc, text);
-    lv_canvas_draw_img(canvas, 62, 21, &bolt, &img_dsc);
+    canvas_draw_text(canvas, 26, 19, 35, &label_right_dsc, text);
+    canvas_draw_img(canvas, 62, 21, &bolt, &img_dsc);
 }
 
 void draw_battery_status(lv_obj_t *canvas, const struct status_state *state) {
     lv_draw_label_dsc_t label_left_dsc;
     init_label_dsc(&label_left_dsc, LVGL_FOREGROUND, &pixel_operator_mono, LV_TEXT_ALIGN_LEFT);
-    lv_canvas_draw_text(canvas, 0, 19, 25, &label_left_dsc, "BAT");
+    canvas_draw_text(canvas, 0, 19, 25, &label_left_dsc, "BAT");
 
     if (state->charging) {
         draw_charging_level(canvas, state);
diff --git a/boards/shields/nice_view_gem/widgets/layer.c b/boards/shields/nice_view_gem/widgets/layer.c
index 21c9fcb..d5a072a 100644
--- a/boards/shields/nice_view_gem/widgets/layer.c
+++ b/boards/shields/nice_view_gem/widgets/layer.c
@@ -15,5 +15,5 @@ void draw_layer_status(lv_obj_t *canvas, const struct status_state *state) {
         to_uppercase(text);
     }
 
-    lv_canvas_draw_text(canvas, 0, 146 + BUFFER_OFFSET_BOTTOM, 68, &label_dsc, text);
+    canvas_draw_text(canvas, 0, 146 + BUFFER_OFFSET_BOTTOM, 68, &label_dsc, text);
 }
\ No newline at end of file
diff --git a/boards/shields/nice_view_gem/widgets/output.c b/boards/shields/nice_view_gem/widgets/output.c
index a63a7e6..a3ca8eb 100644
--- a/boards/shields/nice_view_gem/widgets/output.c
+++ b/boards/shields/nice_view_gem/widgets/output.c
@@ -9,42 +9,42 @@ LV_IMG_DECLARE(usb);
 
 #if !IS_ENABLED(CONFIG_ZMK_SPLIT) || IS_ENABLED(CONFIG_ZMK_SPLIT_ROLE_CENTRAL)
 static void draw_usb_connected(lv_obj_t *canvas) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 45, 2, &usb, &img_dsc);
+    canvas_draw_img(canvas, 45, 2, &usb, &img_dsc);
 }
 
 static void draw_ble_unbonded(lv_obj_t *canvas) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 44, 0, &bt_unbonded, &img_dsc);
+    canvas_draw_img(canvas, 44, 0, &bt_unbonded, &img_dsc);
 }
 #endif
 
 static void draw_ble_disconnected(lv_obj_t *canvas) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 49, 0, &bt_no_signal, &img_dsc);
+    canvas_draw_img(canvas, 49, 0, &bt_no_signal, &img_dsc);
 }
 
 static void draw_ble_connected(lv_obj_t *canvas) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 49, 0, &bt, &img_dsc);
+    canvas_draw_img(canvas, 49, 0, &bt, &img_dsc);
 }
 
 void draw_output_status(lv_obj_t *canvas, const struct status_state *state) {
     lv_draw_label_dsc_t label_dsc;
     init_label_dsc(&label_dsc, LVGL_FOREGROUND, &pixel_operator_mono, LV_TEXT_ALIGN_LEFT);
-    lv_canvas_draw_text(canvas, 0, 1, 25, &label_dsc, "SIG");
+    canvas_draw_text(canvas, 0, 1, 25, &label_dsc, "SIG");
 
     lv_draw_rect_dsc_t rect_white_dsc;
     init_rect_dsc(&rect_white_dsc, LVGL_FOREGROUND);
-    lv_canvas_draw_rect(canvas, 43, 0, 24, 15, &rect_white_dsc);
+    canvas_draw_rect(canvas, 43, 0, 24, 15, &rect_white_dsc);
 
 #if !IS_ENABLED(CONFIG_ZMK_SPLIT) || IS_ENABLED(CONFIG_ZMK_SPLIT_ROLE_CENTRAL)
     switch (state->selected_endpoint.transport) {
diff --git a/boards/shields/nice_view_gem/widgets/profile.c b/boards/shields/nice_view_gem/widgets/profile.c
index 9723e80..0ecb3ec 100644
--- a/boards/shields/nice_view_gem/widgets/profile.c
+++ b/boards/shields/nice_view_gem/widgets/profile.c
@@ -4,10 +4,10 @@
 LV_IMG_DECLARE(profiles);
 
 static void draw_inactive_profiles(lv_obj_t *canvas, const struct status_state *state) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 18, 129 + BUFFER_OFFSET_BOTTOM, &profiles, &img_dsc);
+    canvas_draw_img(canvas, 18, 129 + BUFFER_OFFSET_BOTTOM, &profiles, &img_dsc);
 }
 
 static void draw_active_profile(lv_obj_t *canvas, const struct status_state *state) {
@@ -16,7 +16,7 @@ static void draw_active_profile(lv_obj_t *canvas, const struct status_state *sta
 
     int offset = state->active_profile_index * 7;
 
-    lv_canvas_draw_rect(canvas, 18 + offset, 129 + BUFFER_OFFSET_BOTTOM, 3, 3, &rect_white_dsc);
+    canvas_draw_rect(canvas, 18 + offset, 129 + BUFFER_OFFSET_BOTTOM, 3, 3, &rect_white_dsc);
 }
 
 void draw_profile_status(lv_obj_t *canvas, const struct status_state *state) {
diff --git a/boards/shields/nice_view_gem/widgets/screen.c b/boards/shields/nice_view_gem/widgets/screen.c
index 91f9722..d547a26 100644
--- a/boards/shields/nice_view_gem/widgets/screen.c
+++ b/boards/shields/nice_view_gem/widgets/screen.c
@@ -206,15 +206,15 @@ int zmk_widget_screen_init(struct zmk_widget_screen *widget, lv_obj_t *parent) {
 
     lv_obj_t *top = lv_canvas_create(widget->obj);
     lv_obj_align(top, LV_ALIGN_TOP_RIGHT, 0, 0);
-    lv_canvas_set_buffer(top, widget->cbuf, BUFFER_SIZE, BUFFER_SIZE, LV_IMG_CF_TRUE_COLOR);
+    lv_canvas_set_buffer(top, widget->cbuf, BUFFER_SIZE, BUFFER_SIZE, LV_COLOR_FORMAT_NATIVE);
 
     lv_obj_t *middle = lv_canvas_create(widget->obj);
     lv_obj_align(middle, LV_ALIGN_TOP_RIGHT, BUFFER_OFFSET_MIDDLE, 0);
-    lv_canvas_set_buffer(middle, widget->cbuf2, BUFFER_SIZE, BUFFER_SIZE, LV_IMG_CF_TRUE_COLOR);
+    lv_canvas_set_buffer(middle, widget->cbuf2, BUFFER_SIZE, BUFFER_SIZE, LV_COLOR_FORMAT_NATIVE);
 
     lv_obj_t *bottom = lv_canvas_create(widget->obj);
     lv_obj_align(bottom, LV_ALIGN_TOP_RIGHT, BUFFER_OFFSET_BOTTOM, 0);
-    lv_canvas_set_buffer(bottom, widget->cbuf3, BUFFER_SIZE, BUFFER_SIZE, LV_IMG_CF_TRUE_COLOR);
+    lv_canvas_set_buffer(bottom, widget->cbuf3, BUFFER_SIZE, BUFFER_SIZE, LV_COLOR_FORMAT_NATIVE);
 
     sys_slist_append(&widgets, &widget->node);
     widget_battery_status_init();
diff --git a/boards/shields/nice_view_gem/widgets/screen_peripheral.c b/boards/shields/nice_view_gem/widgets/screen_peripheral.c
index ccf1cdb..a504cc4 100644
--- a/boards/shields/nice_view_gem/widgets/screen_peripheral.c
+++ b/boards/shields/nice_view_gem/widgets/screen_peripheral.c
@@ -109,7 +109,7 @@ int zmk_widget_screen_init(struct zmk_widget_screen *widget, lv_obj_t *parent) {
 
     lv_obj_t *top = lv_canvas_create(widget->obj);
     lv_obj_align(top, LV_ALIGN_TOP_RIGHT, 0, 0);
-    lv_canvas_set_buffer(top, widget->cbuf, BUFFER_SIZE, BUFFER_SIZE, LV_IMG_CF_TRUE_COLOR);
+    lv_canvas_set_buffer(top, widget->cbuf, BUFFER_SIZE, BUFFER_SIZE, LV_COLOR_FORMAT_NATIVE);
 
     draw_animation(widget->obj);
 
diff --git a/boards/shields/nice_view_gem/widgets/util.c b/boards/shields/nice_view_gem/widgets/util.c
index eda0c46..1968cde 100644
--- a/boards/shields/nice_view_gem/widgets/util.c
+++ b/boards/shields/nice_view_gem/widgets/util.c
@@ -9,27 +9,83 @@ void to_uppercase(char *str) {
 }
 
 void rotate_canvas(lv_obj_t *canvas, lv_color_t cbuf[]) {
-    static lv_color_t cbuf_tmp[BUFFER_SIZE * BUFFER_SIZE];
-    memcpy(cbuf_tmp, cbuf, sizeof(cbuf_tmp));
+    /* LVGL 9: lv_canvas_transform is gone. Use lv_draw_sw_rotate on the buffer. */
+    uint8_t *buf = lv_canvas_get_draw_buf(canvas)->data;
+    static uint8_t buf_copy[BUFFER_SIZE * BUFFER_SIZE * sizeof(lv_color_t)];
+    memcpy(buf_copy, buf, sizeof(buf_copy));
 
-    lv_img_dsc_t img;
-    img.data = (void *)cbuf_tmp;
-    img.header.cf = LV_IMG_CF_TRUE_COLOR;
-    img.header.w = BUFFER_SIZE;
-    img.header.h = BUFFER_SIZE;
-
-    lv_canvas_fill_bg(canvas, LVGL_BACKGROUND, LV_OPA_COVER);
-    lv_canvas_transform(canvas, &img, 900, LV_IMG_ZOOM_NONE, -1, 0, BUFFER_SIZE / 2,
-                        BUFFER_SIZE / 2, false);
+    const lv_color_format_t cf = LV_COLOR_FORMAT_NATIVE;
+    const uint32_t stride = lv_draw_buf_width_to_stride(BUFFER_SIZE, cf);
+    lv_draw_sw_rotate(buf_copy, buf, BUFFER_SIZE, BUFFER_SIZE, stride, stride,
+                      LV_DISPLAY_ROTATION_270, cf);
 }
 
 void fill_background(lv_obj_t *canvas) {
     lv_draw_rect_dsc_t rect_black_dsc;
     init_rect_dsc(&rect_black_dsc, LVGL_BACKGROUND);
+    canvas_draw_rect(canvas, 0, 0, BUFFER_SIZE, BUFFER_SIZE, &rect_black_dsc);
+}
+
+/* ---- LVGL-8-shaped wrappers backed by LVGL 9 layer API ---- */
+
+void canvas_draw_rect(lv_obj_t *canvas, int32_t x, int32_t y, int32_t w, int32_t h,
+                      lv_draw_rect_dsc_t *draw_dsc) {
+    lv_layer_t layer;
+    lv_canvas_init_layer(canvas, &layer);
+    lv_area_t coords = {x, y, x + w - 1, y + h - 1};
+    lv_draw_rect(&layer, draw_dsc, &coords);
+    lv_canvas_finish_layer(canvas, &layer);
+}
+
+void canvas_draw_text(lv_obj_t *canvas, int32_t x, int32_t y, int32_t max_w,
+                      lv_draw_label_dsc_t *draw_dsc, const char *txt) {
+    lv_layer_t layer;
+    lv_canvas_init_layer(canvas, &layer);
+    draw_dsc->text = txt;
+    lv_area_t coords = {x, y, x + max_w, y + BUFFER_SIZE};
+    lv_draw_label(&layer, draw_dsc, &coords);
+    lv_canvas_finish_layer(canvas, &layer);
+}
 
-    lv_canvas_draw_rect(canvas, 0, 0, BUFFER_SIZE, BUFFER_SIZE, &rect_black_dsc);
+void canvas_draw_arc(lv_obj_t *canvas, int32_t x, int32_t y, int32_t r, int32_t start_angle,
+                     int32_t end_angle, lv_draw_arc_dsc_t *draw_dsc) {
+    lv_layer_t layer;
+    lv_canvas_init_layer(canvas, &layer);
+    draw_dsc->center.x = x;
+    draw_dsc->center.y = y;
+    draw_dsc->radius = r;
+    draw_dsc->start_angle = start_angle;
+    draw_dsc->end_angle = end_angle;
+    lv_draw_arc(&layer, draw_dsc);
+    lv_canvas_finish_layer(canvas, &layer);
 }
 
+void canvas_draw_line(lv_obj_t *canvas, const lv_point_t points[], uint32_t point_cnt,
+                      lv_draw_line_dsc_t *draw_dsc) {
+    lv_layer_t layer;
+    lv_canvas_init_layer(canvas, &layer);
+    for (uint32_t i = 1; i < point_cnt; ++i) {
+        draw_dsc->p1.x = points[i - 1].x;
+        draw_dsc->p1.y = points[i - 1].y;
+        draw_dsc->p2.x = points[i].x;
+        draw_dsc->p2.y = points[i].y;
+        lv_draw_line(&layer, draw_dsc);
+    }
+    lv_canvas_finish_layer(canvas, &layer);
+}
+
+void canvas_draw_img(lv_obj_t *canvas, int32_t x, int32_t y, const lv_image_dsc_t *src,
+                     lv_draw_image_dsc_t *draw_dsc) {
+    lv_layer_t layer;
+    lv_canvas_init_layer(canvas, &layer);
+    draw_dsc->src = src;
+    lv_area_t coords = {x, y, x + src->header.w - 1, y + src->header.h - 1};
+    lv_draw_image(&layer, draw_dsc, &coords);
+    lv_canvas_finish_layer(canvas, &layer);
+}
+
+/* ---- Descriptor init helpers (unchanged between LVGL 8 and 9) ---- */
+
 void init_label_dsc(lv_draw_label_dsc_t *label_dsc, lv_color_t color, const lv_font_t *font,
                     lv_text_align_t align) {
     lv_draw_label_dsc_init(label_dsc);
@@ -47,4 +103,4 @@ void init_line_dsc(lv_draw_line_dsc_t *line_dsc, lv_color_t color, uint8_t width
     lv_draw_line_dsc_init(line_dsc);
     line_dsc->color = color;
     line_dsc->width = width;
-}
\ No newline at end of file
+}
diff --git a/boards/shields/nice_view_gem/widgets/util.h b/boards/shields/nice_view_gem/widgets/util.h
index 68bb4de..201c263 100644
--- a/boards/shields/nice_view_gem/widgets/util.h
+++ b/boards/shields/nice_view_gem/widgets/util.h
@@ -37,4 +37,16 @@ void fill_background(lv_obj_t *canvas);
 void init_rect_dsc(lv_draw_rect_dsc_t *rect_dsc, lv_color_t bg_color);
 void init_line_dsc(lv_draw_line_dsc_t *line_dsc, lv_color_t color, uint8_t width);
 void init_label_dsc(lv_draw_label_dsc_t *label_dsc, lv_color_t color, const lv_font_t *font,
-                    lv_text_align_t align);
\ No newline at end of file
+                    lv_text_align_t align);
+
+/* LVGL-8-shaped wrappers backed by LVGL 9 layer API (defined in util.c). */
+void canvas_draw_rect(lv_obj_t *canvas, int32_t x, int32_t y, int32_t w, int32_t h,
+                      lv_draw_rect_dsc_t *draw_dsc);
+void canvas_draw_text(lv_obj_t *canvas, int32_t x, int32_t y, int32_t max_w,
+                      lv_draw_label_dsc_t *draw_dsc, const char *txt);
+void canvas_draw_arc(lv_obj_t *canvas, int32_t x, int32_t y, int32_t r, int32_t start_angle,
+                     int32_t end_angle, lv_draw_arc_dsc_t *draw_dsc);
+void canvas_draw_line(lv_obj_t *canvas, const lv_point_t points[], uint32_t point_cnt,
+                      lv_draw_line_dsc_t *draw_dsc);
+void canvas_draw_img(lv_obj_t *canvas, int32_t x, int32_t y, const lv_image_dsc_t *src,
+                     lv_draw_image_dsc_t *draw_dsc);
\ No newline at end of file
diff --git a/boards/shields/nice_view_gem/widgets/wpm.c b/boards/shields/nice_view_gem/widgets/wpm.c
index 0b03fb6..a9acd6d 100644
--- a/boards/shields/nice_view_gem/widgets/wpm.c
+++ b/boards/shields/nice_view_gem/widgets/wpm.c
@@ -7,10 +7,10 @@ LV_IMG_DECLARE(gauge);
 LV_IMG_DECLARE(grid);
 
 static void draw_gauge(lv_obj_t *canvas, const struct status_state *state) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 16, 44 + BUFFER_OFFSET_MIDDLE, &gauge, &img_dsc);
+    canvas_draw_img(canvas, 16, 44 + BUFFER_OFFSET_MIDDLE, &gauge, &img_dsc);
 }
 
 static void draw_needle(lv_obj_t *canvas, const struct status_state *state) {
@@ -49,14 +49,14 @@ static void draw_needle(lv_obj_t *canvas, const struct status_state *state) {
     int needleEndY = centerY + (int)(radius * sin(angleRad));
 
     lv_point_t points[2] = {{needleStartX, needleStartY}, {needleEndX, needleEndY}};
-    lv_canvas_draw_line(canvas, points, 2, &line_dsc);
+    canvas_draw_line(canvas, points, 2, &line_dsc);
 }
 
 static void draw_grid(lv_obj_t *canvas) {
-    lv_draw_img_dsc_t img_dsc;
+    lv_draw_image_dsc_t img_dsc;
     lv_draw_img_dsc_init(&img_dsc);
 
-    lv_canvas_draw_img(canvas, 0, 65 + BUFFER_OFFSET_MIDDLE, &grid, &img_dsc);
+    canvas_draw_img(canvas, 0, 65 + BUFFER_OFFSET_MIDDLE, &grid, &img_dsc);
 }
 
 static void draw_graph(lv_obj_t *canvas, const struct status_state *state) {
@@ -105,13 +105,13 @@ static void draw_graph(lv_obj_t *canvas, const struct status_state *state) {
     }
 #endif
 
-    lv_canvas_draw_line(canvas, points, 10, &line_dsc);
+    canvas_draw_line(canvas, points, 10, &line_dsc);
 }
 
 static void draw_label(lv_obj_t *canvas, const struct status_state *state) {
     lv_draw_label_dsc_t label_left_dsc;
     init_label_dsc(&label_left_dsc, LVGL_FOREGROUND, &pixel_operator_mono, LV_TEXT_ALIGN_LEFT);
-    lv_canvas_draw_text(canvas, 0, 101 + BUFFER_OFFSET_MIDDLE, 25, &label_left_dsc, "WPM");
+    canvas_draw_text(canvas, 0, 101 + BUFFER_OFFSET_MIDDLE, 25, &label_left_dsc, "WPM");
 
     lv_draw_label_dsc_t label_dsc_wpm;
     init_label_dsc(&label_dsc_wpm, LVGL_FOREGROUND, &pixel_operator_mono, LV_TEXT_ALIGN_RIGHT);
@@ -119,7 +119,7 @@ static void draw_label(lv_obj_t *canvas, const struct status_state *state) {
     char wpm_text[6] = {};
 
     snprintf(wpm_text, sizeof(wpm_text), "%d", state->wpm[9]);
-    lv_canvas_draw_text(canvas, 26, 101 + BUFFER_OFFSET_MIDDLE, 42, &label_dsc_wpm, wpm_text);
+    canvas_draw_text(canvas, 26, 101 + BUFFER_OFFSET_MIDDLE, 42, &label_dsc_wpm, wpm_text);
 }
 
 void draw_wpm_status(lv_obj_t *canvas, const struct status_state *state) {
-- 
2.50.1 (Apple Git-155)

